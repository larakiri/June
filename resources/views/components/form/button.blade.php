<x-form.field>
    <button type="submit" 
        class="bg-blue-300 text-white uppercase font-semibold text-xs py-2 px-10 rounded-2xl hover:bg-blue-300"
    > 
        {{ $slot}}
    </button>
</x-form.field>